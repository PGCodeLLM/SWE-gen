apiVersion: v1
kind: Namespace
metadata:
  name: swegen-pipeline
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: swegen-pipeline-config
  namespace: swegen-pipeline
data:
  SWEGEN_PG_HOST: "7.237.95.141"
  SWEGEN_PG_PORT: "5432"
  SWEGEN_PG_USER: root
  SWEGEN_PG_DB: swegen_distributed
  SWEGEN_PG_POOL_MIN: "1"
  SWEGEN_PG_POOL_MAX: "3"
  SWEGEN_LEDGER_BACKEND: postgres
  SWEGEN_WORKSPACE_ROOT: /data/swegen-k3s/workspaces
  SWEGEN_REPO_CACHE_DIR: /data/swegen-k3s/cache/repos
  SWEGEN_CONFIG: /etc/swegen/swegen.toml
  SWEGEN_PROXY_CA_BUNDLE: /etc/swegen/combined-ca.crt
  SSL_CERT_FILE: /etc/swegen/combined-ca.crt
  REQUESTS_CA_BUNDLE: /etc/swegen/combined-ca.crt
  CURL_CA_BUNDLE: /etc/swegen/combined-ca.crt
  GIT_SSL_CAINFO: /etc/swegen/combined-ca.crt
  NODE_EXTRA_CA_CERTS: /etc/swegen/combined-ca.crt
  SWEGEN_CC_TIMEOUT_SECONDS: "10800"
  SWEGEN_GENERATE_TIMEOUT_SECONDS: "14400"
  SWEGEN_GITHUB_API_ATTEMPTS: "8"
  SWEGEN_GITHUB_RETRY_BASE_SECONDS: "10"
  SWEGEN_GITHUB_MAX_WAIT_SECONDS: "300"
  SWEGEN_HARBOR_TIMEOUT_SECONDS: "3600"
  SWEGEN_REPAIR_TIMEOUT_SECONDS: "14400"
  SWEGEN_MAX_REPAIR_ATTEMPTS: "3"
  SWEGEN_REWARD_PRIMARY_MODEL: gpt-5.6-sol
  SWEGEN_REWARD_FALLBACK_MODEL: gpt-5.6-sol
  SWEGEN_SWR_HOST: swr-coder-data-platform-wce1sr.swr-pro.myhuaweicloud.com
  SWEGEN_SWR_REPOSITORY: swesandbox/public/swe-gen/feature-implementation/generated
  SWEGEN_SWR_REGISTRY: platform
  SWEGEN_SWR_SUFFIX: _platform
  # Enable hybrid after the remote farm's server-side SWR credentials pass a push smoke.
  SWEGEN_BUILD_ROUTER_MODE: hybrid
  SWEGEN_REMOTE_BUILDKIT_URL: http://7.156.122.134:32083
  SWEGEN_REMOTE_BUILDKIT_REGISTRY: swr-coder-data-platform-wce1sr.swr-pro.myhuaweicloud.com
  SWEGEN_REMOTE_BUILDKIT_PULL_REGISTRY_URL: swr-coder-data-platform-wce1sr.swr-pro.myhuaweicloud.com/swesandbox
  SWEGEN_REMOTE_BUILDKIT_REPOSITORY: swesandbox/public/swe-gen/feature-implementation/generated
  SWEGEN_REMOTE_BUILDKIT_CALLBACK_URL: http://callback-server.buildkit-isolated.svc.cluster.local:9000/callback
  SWEGEN_REMOTE_BUILDKIT_BASE_IMAGE_SOURCE_REGISTRY: swr.cn-southwest-2.myhuaweicloud.com
  SWEGEN_REMOTE_BUILDKIT_BASE_IMAGE_MIRROR_REGISTRY: swr-coder-data-platform-wce1sr.swr-pro.myhuaweicloud.com
  SWEGEN_REMOTE_BUILDKIT_PERCENT: "75"
  SWEGEN_REMOTE_BUILDKIT_TIMEOUT_SECONDS: "3600"
  SWEGEN_REMOTE_BUILDKIT_FALLBACK_LOCAL: "true"
  SWEGEN_NO_PROXY: ".myhuaweicloud.com,.huaweicloud.com,.huawei.com,127.0.0.1,localhost,7.156.122.134,7.237.95.141,7.244.3.251,7.244.3.200,7.244.3.78,7.244.2.110,7.244.1.209,10.42.0.0/16,10.43.0.0/16,100.*,10.*"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swegen-generate
  namespace: swegen-pipeline
spec:
  replicas: 48
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: swegen-worker
      swegen.pgcode/stage: generate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swegen-worker
        swegen.pgcode/stage: generate
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 18000
      nodeSelector:
        swegen.pgcode/node-ip: 7.244.2.110
      containers:
        - name: worker
          image: swegen-worker:e2e
          imagePullPolicy: Never
          args: ["--stage", "generate"]
          envFrom:
            - configMapRef:
                name: swegen-pipeline-config
            - secretRef:
                name: swegen-database
            - secretRef:
                name: swegen-runtime-proxy
            - secretRef:
                name: swegen-model-credentials
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: NO_PROXY
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
            - name: no_proxy
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
          resources:
            requests:
              cpu: 500m
              memory: 1536Mi
            limits:
              cpu: "16"
              memory: 32Gi
          volumeMounts:
            - name: workspace
              mountPath: /data/swegen-k3s/workspaces
            - name: repo-cache
              mountPath: /data/swegen-k3s/cache
            - name: private-files
              mountPath: /etc/swegen/swegen.toml
              subPath: swegen.toml
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/combined-ca.crt
              subPath: combined-ca.crt
              readOnly: true
      volumes:
        - name: workspace
          hostPath:
            path: /data/swegen-k3s/workspaces
            type: DirectoryOrCreate
        - name: repo-cache
          hostPath:
            path: /data/swegen-k3s/cache
            type: DirectoryOrCreate
        - name: private-files
          secret:
            secretName: swegen-private-files
            defaultMode: 0400
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swegen-validate
  namespace: swegen-pipeline
spec:
  replicas: 96
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 20%
  selector:
    matchLabels:
      app.kubernetes.io/name: swegen-worker
      swegen.pgcode/stage: validate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swegen-worker
        swegen.pgcode/stage: validate
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 300
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: swegen-worker
              swegen.pgcode/stage: validate
      containers:
        - name: worker
          image: swegen-worker:hybrid-buildkit-proxy-skip-20260730
          imagePullPolicy: Never
          args: ["--stage", "validate"]
          envFrom:
            - configMapRef:
                name: swegen-pipeline-config
            - secretRef:
                name: swegen-database
            - secretRef:
                name: swegen-runtime-proxy
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: DOCKER_BUILDKIT
              value: "1"
            - name: BUILDX_BUILDER
              value: "default"
            - name: COMPOSE_BAKE
              value: "false"
            - name: PATH
              value: /opt/swegen/bin:/app/.venv/bin:/usr/local/bin:/usr/local/sbin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: SWEGEN_BUILD_SLOT_DIR
              value: /run/swegen-build-slots
            - name: SWEGEN_REAL_DOCKER
              value: /usr/bin/docker
            - name: NO_PROXY
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
            - name: no_proxy
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
          resources:
            requests:
              cpu: 250m
              memory: 1200Mi
            limits:
              cpu: "8"
              memory: 16Gi
          volumeMounts:
            - name: workspace
              mountPath: /data/swegen-k3s/workspaces
            - name: docker-sock
              mountPath: /var/run/docker.sock
            - name: build-slots
              mountPath: /run/swegen-build-slots
            - name: swegen-bin
              mountPath: /opt/swegen/bin
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/swegen.toml
              subPath: swegen.toml
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/combined-ca.crt
              subPath: combined-ca.crt
              readOnly: true
            - name: docker-config
              mountPath: /root/.docker/config.json
              subPath: config.json
              readOnly: true
      volumes:
        - name: workspace
          hostPath:
            path: /data/swegen-k3s/workspaces
            type: DirectoryOrCreate
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: build-slots
          hostPath:
            path: /data/swegen-k3s/build-slots
            type: DirectoryOrCreate
        - name: swegen-bin
          hostPath:
            path: /data/swegen-k3s/bin
            type: DirectoryOrCreate
        - name: private-files
          secret:
            secretName: swegen-private-files
            defaultMode: 0400
        - name: docker-config
          secret:
            secretName: swegen-docker-config
            defaultMode: 0400
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swegen-repair
  namespace: swegen-pipeline
spec:
  replicas: 4
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 20%
  selector:
    matchLabels:
      app.kubernetes.io/name: swegen-worker
      swegen.pgcode/stage: repair
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swegen-worker
        swegen.pgcode/stage: repair
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 18000
      containers:
        - name: worker
          image: swegen-worker:hybrid-buildkit-proxy-skip-20260730
          imagePullPolicy: Never
          args: ["--stage", "repair"]
          envFrom:
            - configMapRef:
                name: swegen-pipeline-config
            - secretRef:
                name: swegen-database
            - secretRef:
                name: swegen-runtime-proxy
            - secretRef:
                name: swegen-repair-model-credentials
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: DOCKER_BUILDKIT
              value: "1"
            - name: BUILDX_BUILDER
              value: "default"
            - name: COMPOSE_BAKE
              value: "false"
            - name: PATH
              value: /opt/swegen/bin:/app/.venv/bin:/usr/local/bin:/usr/local/sbin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: SWEGEN_BUILD_SLOT_DIR
              value: /run/swegen-build-slots
            - name: SWEGEN_REAL_DOCKER
              value: /usr/bin/docker
            - name: NO_PROXY
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
            - name: no_proxy
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
          resources:
            requests:
              cpu: 250m
              memory: 1200Mi
            limits:
              cpu: "8"
              memory: 16Gi
          volumeMounts:
            - name: workspace
              mountPath: /data/swegen-k3s/workspaces
            - name: docker-sock
              mountPath: /var/run/docker.sock
            - name: build-slots
              mountPath: /run/swegen-build-slots
            - name: swegen-bin
              mountPath: /opt/swegen/bin
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/swegen.toml
              subPath: swegen.toml
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/combined-ca.crt
              subPath: combined-ca.crt
              readOnly: true
            - name: docker-config
              mountPath: /root/.docker/config.json
              subPath: config.json
              readOnly: true
      volumes:
        - name: workspace
          hostPath:
            path: /data/swegen-k3s/workspaces
            type: DirectoryOrCreate
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: build-slots
          hostPath:
            path: /data/swegen-k3s/build-slots
            type: DirectoryOrCreate
        - name: swegen-bin
          hostPath:
            path: /data/swegen-k3s/bin
            type: DirectoryOrCreate
        - name: private-files
          secret:
            secretName: swegen-private-files
            defaultMode: 0400
        - name: docker-config
          secret:
            secretName: swegen-docker-config
            defaultMode: 0400
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swegen-reward
  namespace: swegen-pipeline
spec:
  replicas: 16
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: swegen-worker
      swegen.pgcode/stage: reward
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swegen-worker
        swegen.pgcode/stage: reward
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 1800
      nodeSelector:
        swegen.pgcode/node-ip: 7.244.1.209
      containers:
        - name: worker
          image: swegen-worker:e2e
          imagePullPolicy: Never
          args: ["--stage", "reward"]
          envFrom:
            - configMapRef:
                name: swegen-pipeline-config
            - secretRef:
                name: swegen-database
            - secretRef:
                name: swegen-runtime-proxy
          env:
            - name: SWEGEN_REWARD_API_KEY
              valueFrom:
                secretKeyRef:
                  name: swegen-reward-credentials
                  key: OPENAI_API_KEY
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: NO_PROXY
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
            - name: no_proxy
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 8Gi
          volumeMounts:
            - name: workspace
              mountPath: /data/swegen-k3s/workspaces
            - name: private-files
              mountPath: /etc/swegen/swegen.toml
              subPath: swegen.toml
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/combined-ca.crt
              subPath: combined-ca.crt
              readOnly: true
      volumes:
        - name: workspace
          hostPath:
            path: /data/swegen-k3s/workspaces
            type: DirectoryOrCreate
        - name: private-files
          secret:
            secretName: swegen-private-files
            defaultMode: 0400
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: swegen-push
  namespace: swegen-pipeline
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: swegen-worker
      swegen.pgcode/stage: push
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swegen-worker
        swegen.pgcode/stage: push
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 7200
      nodeSelector:
        swegen.pgcode/node-ip: 7.244.3.200
      containers:
        - name: worker
          image: swegen-worker:hybrid-buildkit-proxy-skip-20260730
          imagePullPolicy: Never
          args: ["--stage", "push"]
          envFrom:
            - configMapRef:
                name: swegen-pipeline-config
            - secretRef:
                name: swegen-database
            - secretRef:
                name: swegen-runtime-proxy
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: DOCKER_BUILDKIT
              value: "1"
            - name: BUILDX_BUILDER
              value: "default"
            - name: COMPOSE_BAKE
              value: "false"
            - name: PATH
              value: /opt/swegen/bin:/app/.venv/bin:/usr/local/bin:/usr/local/sbin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: SWEGEN_BUILD_SLOT_DIR
              value: /run/swegen-build-slots
            - name: SWEGEN_REAL_DOCKER
              value: /usr/bin/docker
            - name: NO_PROXY
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
            - name: no_proxy
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
          resources:
            requests:
              cpu: "2"
              memory: 200Mi
            limits:
              cpu: "8"
              memory: 16Gi
          volumeMounts:
            - name: workspace
              mountPath: /data/swegen-k3s/workspaces
            - name: docker-sock
              mountPath: /var/run/docker.sock
            - name: build-slots
              mountPath: /run/swegen-build-slots
            - name: swegen-bin
              mountPath: /opt/swegen/bin
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/swegen.toml
              subPath: swegen.toml
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/combined-ca.crt
              subPath: combined-ca.crt
              readOnly: true
            - name: docker-config
              mountPath: /root/.docker/config.json
              subPath: config.json
              readOnly: true
      volumes:
        - name: workspace
          hostPath:
            path: /data/swegen-k3s/workspaces
            type: DirectoryOrCreate
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: build-slots
          hostPath:
            path: /data/swegen-k3s/build-slots
            type: DirectoryOrCreate
        - name: swegen-bin
          hostPath:
            path: /data/swegen-k3s/bin
            type: DirectoryOrCreate
        - name: private-files
          secret:
            secretName: swegen-private-files
            defaultMode: 0400
        - name: docker-config
          secret:
            secretName: swegen-docker-config
            defaultMode: 0400
