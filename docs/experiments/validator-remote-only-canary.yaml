apiVersion: apps/v1
kind: Deployment
metadata:
  name: swegen-validate-remote-only-canary
  namespace: swegen-pipeline
  labels:
    swegen.pgcode/experiment: remote-only-buildfarm-20260802
spec:
  # Completed experiment: remote-only routing improved infrastructure
  # reliability but produced only 7.7% Validate yield. Leave disabled; see
  # experiment 18 in docs/verifier_yield_improvement.md.
  replicas: 0
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 25%
  selector:
    matchLabels:
      app.kubernetes.io/name: swegen-worker-remote-only-canary
      swegen.pgcode/stage: validate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: swegen-worker-remote-only-canary
        swegen.pgcode/stage: validate
        swegen.pgcode/experiment: remote-only-buildfarm-20260802
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 600
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: swegen.pgcode/node-ip
                    operator: In
                    values:
                      - 7.244.3.200
                      - 7.244.2.110
                      - 7.244.1.209
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: swegen-worker-remote-only-canary
              swegen.pgcode/stage: validate
      containers:
        - name: worker
          image: swegen-worker:repo-affinity-runbox-20260802-0049
          imagePullPolicy: Never
          args: ["--stage", "validate"]
          envFrom:
            - configMapRef:
                name: swegen-pipeline-config
            - secretRef:
                name: swegen-database
            - secretRef:
                name: swegen-runtime-proxy
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            # This is the experiment's only routing change. Production
            # validators continue to fill node-local slots and then overflow;
            # this cohort sends every image build to the remote farm and never
            # falls back into the node-local queue after a remote failure.
            - name: SWEGEN_BUILD_ROUTER_MODE
              value: remote
            - name: SWEGEN_REMOTE_BUILDKIT_FALLBACK_LOCAL
              value: "false"
            - name: DOCKER_BUILDKIT
              value: "1"
            - name: BUILDX_BUILDER
              value: default
            - name: COMPOSE_BAKE
              value: "false"
            - name: PATH
              value: /opt/swegen/bin:/app/.venv/bin:/usr/local/bin:/usr/local/sbin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: SWEGEN_BUILD_SLOT_DIR
              value: /run/swegen-build-slots
            - name: SWEGEN_REAL_DOCKER
              value: /usr/bin/docker
            - name: NO_PROXY
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
            - name: no_proxy
              valueFrom:
                configMapKeyRef:
                  name: swegen-pipeline-config
                  key: SWEGEN_NO_PROXY
          resources:
            requests:
              cpu: 250m
              memory: 1200Mi
            limits:
              cpu: "8"
              memory: 16Gi
          volumeMounts:
            - name: workspace
              mountPath: /data/swegen-k3s/workspaces
            - name: docker-sock
              mountPath: /var/run/docker.sock
            - name: build-slots
              mountPath: /run/swegen-build-slots
            - name: swegen-bin
              mountPath: /opt/swegen/bin
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/swegen.toml
              subPath: swegen.toml
              readOnly: true
            - name: private-files
              mountPath: /etc/swegen/combined-ca.crt
              subPath: combined-ca.crt
              readOnly: true
            - name: docker-config
              mountPath: /root/.docker/config.json
              subPath: config.json
              readOnly: true
      volumes:
        - name: workspace
          hostPath:
            path: /data/swegen-k3s/workspaces
            type: DirectoryOrCreate
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: build-slots
          hostPath:
            path: /data/swegen-k3s/build-slots
            type: DirectoryOrCreate
        - name: swegen-bin
          hostPath:
            path: /data/swegen-k3s/bin
            type: DirectoryOrCreate
        - name: private-files
          secret:
            secretName: swegen-private-files
            defaultMode: 0400
        - name: docker-config
          secret:
            secretName: swegen-docker-config
            defaultMode: 0400
